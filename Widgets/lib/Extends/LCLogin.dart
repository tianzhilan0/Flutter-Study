import 'package:flutter/material.dart';

class LCLogin extends StatefulWidget {
  LCLogin({Key key}) : super(key: key);

  @override
  _LCLoginState createState() => _LCLoginState();
}

class _LCLoginState extends State<LCLogin> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text("flutter_swiper")),
        body: Container());
  }
}